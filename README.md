# student-score-program
A program to input 10 student score, traverse the array and compute the average, modify the program to insert a new score in the 5th position ,delete the lowest score from the array and implement the linear and binary search for a student's score.
